---
import { Image } from "astro:assets";
import HeroHorizontal from "../assets/images/hero-horizontal.webp"
import HeroVertical from "../assets/images/hero-vertical.webp"
---

<section
  class="h-[100dvh] w-full flex items-center justify-center relative overflow-hidden"
>
  <Image src={HeroHorizontal} alt="Woman getting a treatment" class="hero-bg hidden md:block" />
  <Image src={HeroVertical} alt="Woman getting a treatment" class="hero-bg block md:hidden" />
  <div class="absolute inset-0 bg-black/40"></div>
  <div class="relative text-center text-white p-6 z-10 fade-in is-visible">
    <h1
      class="font-serif text-4xl md:text-6xl lg:text-7xl font-normal tracking-tight leading-tight text-balance"
    >
      Confidence, Redefined.
    </h1>
    <p
      class="mt-4 text-lg md:text-xl max-w-2xl mx-auto font-light text-balance"
    >
      Expert aesthetic medicine for a naturally radiant you. Discover a
      personalized approach to timeless beauty.
    </p>
    <div class="mt-10">
      <a href="#contact" class="btn btn-primary">Book a Consultation</a>
    </div>
  </div>
</section>

<style>
  /* Hero Video Background */
  .hero-bg {
    position: absolute;
    top: 50%;
    left: 50%;
    width: auto;
    height: auto;
    min-width: 100%;
    min-height: 100%;
    transform: translate(-50%, -50%);
    z-index: 0;
    object-fit: cover;
  }
</style>