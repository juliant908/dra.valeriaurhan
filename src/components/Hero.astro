<section class="hero-section min-h-[350px] 2xl:min-h-[700px]">
  <div class="container mx-auto text-center py-20">
    <h1 class="text-4xl font-bold mb-4">Welcome to Our Website</h1>
    <p class="text-lg mb-8">
      Discover the best in aesthetic and anti-aging medicine.
    </p>
    <button
      class="inline-block btn-cta bg-[var(--bg-button-primary)] text-white py-4 min-w-[200px] rounded-lg hover:bg-[var(--btn-hover)] text-base transition duration-300"
    >
      BOOK NOW
    </button>
  </div>
</section>

<style>
  .btn-cta {
    font-family: var(--primary-font);
  }

  .hero-section {
    position: relative;
    background-image: url("../assets/images/hero.webp");
    background-size: cover;
    background-repeat: no-repeat;
    background-position: center;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--light-cream);

    &::after {
      content: "";
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.2);
    }

    * {
      z-index: 1;
    }
  }
</style>
