---
const {class: className = "", id} = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section section-container scroll-m-20 w-full grid place-items-center ${className}`}
>
  <div
    class="xl:max-w-6xl lg:max-w-4xl md:max-w-3xl sm:max-w-xl mx-auto w-full container px-4 sm:px-0"
  >
    <slot />
  </div>
</section>
